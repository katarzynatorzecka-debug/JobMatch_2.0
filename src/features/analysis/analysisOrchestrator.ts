import type { FilteredJobOffer } from '../../contracts/hardFilter'
import type { JobAnalysis } from '../../contracts/jobAnalysis'
import type { UserProfile } from '../../contracts/profile'
import { AIAnalysisService } from './analysisService'
import type { AnalysisRepository } from './analysisRepository'
import { OfferContentProvider } from './offerContentProvider'
export type AnalysisProgress = { offerId: string; state: 'queued' | 'fetching' | 'analyzing' | 'ready' | 'retry' | 'rejected'; error?: string; sourceQuality?: 'full' | 'partial' | 'unavailable' | 'fixture'; sourceErrorCode?: string }
export class AnalysisOrchestrator { constructor(private readonly provider: OfferContentProvider, private readonly service: AIAnalysisService, private readonly repository: AnalysisRepository) {} async analyzeAll(profile: UserProfile, items: FilteredJobOffer[], onProgress: (progress: AnalysisProgress) => void): Promise<JobAnalysis[]> { const results: JobAnalysis[] = []; for (const item of items) { if (item.result.status === 'fail') { onProgress({ offerId: item.offer.id, state: 'rejected' }); continue } onProgress({ offerId: item.offer.id, state: 'fetching' }); const content = await this.provider.find(item.offer); onProgress({ offerId: item.offer.id, state: 'analyzing', sourceQuality: content.sourceQuality, sourceErrorCode: content.sourceErrorCode }); let analysis: JobAnalysis; try { analysis = await this.service.analyze(profile, item, content) } catch (error) { onProgress({ offerId: item.offer.id, state: 'retry', error: error instanceof Error ? error.message : 'ANALYSIS_REQUEST_FAILED', sourceQuality: content.sourceQuality, sourceErrorCode: content.sourceErrorCode }); continue } try { await this.repository.save(analysis) } catch { onProgress({ offerId: item.offer.id, state: 'retry', error: 'ANALYSIS_SAVE_FAILED', sourceQuality: content.sourceQuality, sourceErrorCode: content.sourceErrorCode }); continue } results.push(analysis); onProgress({ offerId: item.offer.id, state: 'ready', sourceQuality: content.sourceQuality, sourceErrorCode: content.sourceErrorCode }) } return results } }
